# X Patterns

This directory contains lists of different groups of object to reduce errors in boilerplate by applying
the X pattern.
Due to naming consistency, all lists must be of the form:

```c
X(SomeName, SOME_NAME, some_name, s_name, some_names)
```
